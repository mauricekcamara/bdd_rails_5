<%= link_to "New Article", new_article_path, class: "btn btn-default btn-lg" %>

<% @articles.each do |article| %>
  <div class="well well-lg">
    <%= link_to article.title, article_path(article) %>
    <%= truncate(article.body, length: 100) %>
  </div>
  
<% end %>